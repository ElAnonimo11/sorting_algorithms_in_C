#ifndef _INSERTION_SORT_H_
#define _INSERTION_SORT_H_

/*
 * In insertion sort, we search for each element's position
 * at the beginning of the already sorted array.
 */

void insertion_sort(int vector[],int tam_vector);

#endif
